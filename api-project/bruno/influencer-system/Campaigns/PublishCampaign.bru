meta {
  name: Publish Campaign
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/campaigns/{{campaignId}}/publish
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.data.state: eq published
}
