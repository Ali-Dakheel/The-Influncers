meta {
  name: Pause Campaign
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/campaigns/{{campaignId}}/pause
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.data.state: eq paused
}
