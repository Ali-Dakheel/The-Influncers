meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 204
}
