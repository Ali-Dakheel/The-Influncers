meta {
  name: My Pitches
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/pitches/mine
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
}
