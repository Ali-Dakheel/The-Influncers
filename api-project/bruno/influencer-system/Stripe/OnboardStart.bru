meta {
  name: Start Stripe Onboarding
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/me/stripe/onboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.account_id: isString
}
