meta {
  name: List Drafts for Application
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/applications/{{applicationId}}/drafts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
}
